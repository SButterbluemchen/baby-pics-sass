{
  "name": "baby-pics-sass",
  "version": "0.0.1",
  "description": "baby-pics",
  "keywords": ["baby pics", "photographer"],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch stylesheets/main.scss stylesheets/main.css"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
